# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.coverage
coverage.xml
dist/

# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
crash.log
# Lock files carry per-platform hashes; this tree is validate-only, so locks
# are regenerated by `init` in CI instead of being committed.
.terraform.lock.hcl

# Tooling caches
.ruff_cache/
.mypy_cache/
node_modules/

# Local environment
.env
.env.*
!.env.example
kubeconfig*

# OS noise
.DS_Store
Thumbs.db

# Terraform plugin cache (local)
.tfcache/
