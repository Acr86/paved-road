# Pinned tool and component versions for the local platform.
# bootstrap.sh sources this file; CI reads it too, so local and CI never drift.

K3D_VERSION=v5.8.3
K3S_IMAGE=rancher/k3s:v1.33.4-k3s1
ARGOCD_VERSION=v3.1.8
KUBE_PROMETHEUS_STACK_VERSION=77.14.0

# Cluster identity
CLUSTER_NAME=paved-road
INGRESS_HTTP_PORT=8080
